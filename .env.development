# Dev: chamadas diretas à API (evita depender do proxy /api do Vite).
# Reinicie o Vite após alterar. Produção: ver .env.production (site https://benvil-hacks.vercel.app/).
VITE_API_URL=http://localhost:5261
