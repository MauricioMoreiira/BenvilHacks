# Build de produção (ex.: Vercel). URL pública da API .NET, sem barra no fim.
# Ajuste se a API estiver noutro host; no Vercel pode definir a mesma variável em Settings → Environment Variables.
VITE_API_URL=https://benvilhacks.com
